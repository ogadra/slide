{
  "name": "solo-dev-considerations",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "author": "ogadra",
  "scripts": {
    "build": "npm run build:slidev && npm run build:copy",
    "build:copy": "cp -r ./slides-export ../../dist/solo-dev-considerations",
    "build:slidev": "slidev build --base /solo-dev-considerations/ --out ../../dist/solo-dev-considerations",
    "dev": "slidev --open",
    "export": "slidev export",
    "export:png": "slidev export --format png"
  }
}
