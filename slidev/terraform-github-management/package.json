{
  "name": "terraform-github-management",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "author": "ogadra",
  "scripts": {
    "build": "npm run build:slidev && npm run build:copy",
    "build:copy": "cp -r ./slides-export ../../dist/terraform-github-management",
    "build:slidev": "slidev build --base /terraform-github-management/ --out ../../dist/terraform-github-management",
    "dev": "slidev --open",
    "export": "slidev export",
    "export:png": "slidev export --format png"
  }
}
