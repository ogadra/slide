{
  "name": "terraform-domain-destruction",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "author": "ogadra",
  "scripts": {
    "build": "pnpm run build:slidev && pnpm run build:copy",
    "build:copy": "cp -r ./slides-export ../../dist/terraform-domain-destruction",
    "build:slidev": "slidev build --base /terraform-domain-destruction/ --out ../../dist/terraform-domain-destruction",
    "dev": "slidev --open",
    "export": "slidev export",
    "export:png": "slidev export --format png"
  }
}
