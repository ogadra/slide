{
  "name": "cloudflare-fullstack-application",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "author": "ogadra",
  "scripts": {
    "build": "npm run build:slidev && npm run build:copy",
    "build:copy": "cp -r ./slides-export ../../dist/cloudflare-fullstack-application",
    "build:slidev": "slidev build --base /cloudflare-fullstack-application/ --out ../../dist/cloudflare-fullstack-application",
    "dev": "slidev --open",
    "export": "slidev export",
    "export:png": "slidev export --format png"
  }
}
