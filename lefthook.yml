pre-commit:
  parallel: false
  piped: true
  commands:
    01_typecheck:
      root: "home"
      glob: "*.{ts,tsx}"
      stage_fixed: true
      run: npm run typecheck
    01_secrets:
      run: gitleaks protect --staged -v
    02_fix:
      root: "home"
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,jsonc}"
      stage_fixed: true
      run: npm run lint:fix {staged_files}
    03_check:
      root: "home"
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,jsonc}"
      run: npm run lint {staged_files}
